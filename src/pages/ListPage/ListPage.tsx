import { FC } from 'react';

import styles from './ListPage.module.scss';

export const ListPage: FC = () => {
  return (
    <div className={styles.wrapper}>
      <p>ListPage component</p>
    </div>
  );
};
